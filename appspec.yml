version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu

hooks:
  AfterInstall:
   - location: /scripts/afterInstall.sh
     timeout: 6000
     runas: ubuntu
  ApplicationStart:
   - location: /scripts/applicationStart.sh
     timeout: 6000
     runas: ubuntu